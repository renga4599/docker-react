sudo: required

services:
  - docker

before_install:
  - docker build -t renga4599/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true renga4599/docker-react npm run test

language: generic
